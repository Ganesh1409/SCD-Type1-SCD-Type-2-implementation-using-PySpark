# SCD-Type1-SCD-Type-2-implementation-using-PySpark
SCD Type1 &amp; SCD Type 2 implementation on Data incoming from ADLS gen2 location to  Databricks using PySpark. Storing the final data in Delta format in ADLS gen2(external location) 
